# Frontend Environment Variables - YarnFlow
# Copier ce fichier vers .env, .env.development, .env.staging, .env.production
# et adapter les valeurs pour chaque environnement

# ============================================================================
# VARIABLES REQUISES (obligatoires pour tous les environnements)
# ============================================================================

# API Backend URL (pour les appels API)
VITE_API_URL=http://patron-maker.local/api

# Backend URL (pour charger les images/uploads)
VITE_BACKEND_URL=http://patron-maker.local/api

# Environnement (development, staging, production)
VITE_ENV=development

# ============================================================================
# VARIABLES RECOMMANDÉES
# ============================================================================

# Frontend URL
VITE_FRONTEND_URL=http://localhost:5173

# Stripe - Clé publique (test ou live selon environnement)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_VOTRE_CLE_TEST

# Application Environment
VITE_APP_ENV=development

# Debug mode (true pour dev/staging, false pour production)
VITE_APP_DEBUG=true
